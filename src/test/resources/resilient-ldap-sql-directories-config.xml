<?xml version="1.0"?>
<component name="org.nuxeo.ecm.directory.resilient.config">

  <extension
    target="org.nuxeo.ecm.directory.resilient.ResilientDirectoryFactory"
    point="directories">

    <directory name="resilient">

		<!-- The LDAP user directory (defined in the LDAP test case) -->
        <subDirectory name="userDirectory" master="true">
        </subDirectory>
		
		<!-- The sql slave directory -->
        <subDirectory name="sqlDirectory">
        </subDirectory>
    </directory>


  </extension>

</component>
